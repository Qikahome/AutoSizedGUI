accessWidener v2 named
# common 源码的访问需求（1.20.1 mojmap 下均为 public final / private）：
# 1) Slot.x/y —— AutoSizedContainerScreen/ItemSlot 写槽位坐标（去 final）
# 2) AbstractContainerMenu.slots —— AutoSizedContainerScreen 包装菜单槽、TestContainer quickMove 遍历（private -> accessible）
mutable field net/minecraft/world/inventory/Slot x I
mutable field net/minecraft/world/inventory/Slot y I
accessible field net/minecraft/world/inventory/AbstractContainerMenu slots Lnet/minecraft/core/NonNullList;
